model:
  backbone: resnet50
  pretrained: true
  num_classes: 2

training:
  epochs: 50
  learning_rate: 5.0e-5

wandb:
  tags: [supervised, resnet50]

data:
  train_split: data/splits/train.txt
  val_split: data/splits/val.txt
  labels_file: data/labels.json
