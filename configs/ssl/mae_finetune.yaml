defaults:
  - base

model:
  backbone: vit_base_patch16_224
  num_classes: 2
  pretrained_checkpoint: null  # path to MAE pretrain checkpoint

training:
  epochs: 30
  learning_rate: 1.0e-5

wandb:
  tags: [ssl, ssl-finetune, mae]
